# HexTechBilliards MVP 完成度评估与路线图

> 基于代码库全面审查，评估当前项目离「最小可玩 MVP」的差距，并给出收敛路径。

---

## 1. 当前已完成的能力

| 模块 | 状态 | 说明 |
|------|------|------|
| 首页导航 | ✅ | 本地对战 / 在线对战 / BOSS 占位 |
| 大厅流程 | ✅ | 输入昵称 → 创建/加入房间 → 准备 → 开始 |
| Canvas 渲染 | ✅ | 球桌、球、球杆、瞄准线、击球动画 |
| 物理引擎 | ✅ | 4 子步进 + 3 次碰撞迭代 + 弹性碰撞 + 摩擦 + 库边 + 进袋 |
| 输入控制 | ✅ | 鼠标/触摸 → 瞄准 → 蓄力 → 击球 |
| 中式八球规则 | ✅ | 球组分配、犯规判定、胜负判定、白球自由球 |
| 在线联机框架 | ✅ | WebRTC P2P + WebSocket 信令服务器 + 房间创建/加入 |
| 游戏结束 | ✅（本地） | 胜负弹窗 + 再来一局（仅本地）/ 返回首页 |
| Vercel 部署 | ✅ | Build Output API v3，前端可部署 |
| Render 部署 | ✅ | 信令服务器 render.yaml 配置就绪 |

**核心闭环已打通**：首页 → 选模式 → 瞄准蓄力击球 → 物理模拟 → 规则判定 → 胜负结算 → 再来一局。

---

## 2. 离 MVP 的差距：分优先级

### 🔴 P0 —— 不修就会「卡死 / 玩不下去」

| # | 缺口 | 现状 | 影响 | 修复难度 |
|---|------|------|------|----------|
| 1 | **击球部位（spin）是假的** | `spinType` 只在 CanvasRenderer 里有值，UI 有 9 宫格选择器，但从不参与物理。`shoot()` 只传 power + angle | 玩家点了旋转，球的行为零变化，严重误导 | ⭐ 简单：隐藏控件；⭐⭐⭐ 中等：接入旋转物理 |
| 2 | **在线模式无「再来一局」** | `rematch_request` / `rematch_accept` 全是 `// TODO`，game over 按钮在线模式下隐藏 | 一局结束只能各自回首页，在线体验断裂 | ⭐⭐ 中等 |
| 3 | **对手断线/退出无处理** | `opponent_left` 事件 emit 后，gameStore 不监听，玩家永远卡在「等待对手击球…」 | 对手一走你就死锁 | ⭐ 简单 |
| 4 | **在线物理状态漂移无纠正** | `state_checksum` 检测到漂移 → `// TODO`，无纠正 | 多回合后两端球局必然分叉（浮点累积误差），「你进了我没进」 | ⭐⭐⭐⭐ 困难 |

### 🟡 P1 —— 能玩但体验残缺

| # | 缺口 | 现状 | 影响 |
|---|------|------|------|
| 5 | **瞄准辅助太弱** | 只有一条直线，无库边反弹预测、无目标球落点预览 | 手机端手指遮挡 + 触屏不精确，极难瞄准 |
| 6 | **无音效/触觉反馈** | 击球、碰撞、进袋全静音 | 台球游戏「手感」缺一大半 |
| 7 | **本地换手无提示** | 换手时无遮罩，无「请将手机递给对方」 | 双人同屏容易误操作 |
| 8 | **WebSocket 回退不可用** | WebRTC 失败时回退到 WS relay，但服务器只转发 WebRTC 信令，不转发游戏消息 | 移动端/小程序 WebRTC 受限时在线模式直接哑火 |
| 9 | **ForceField 未接入** | 海克斯力场系统有雏形代码，主流程没用到 | 核心特色功能缺席（但 MVP 可暂缓） |

### 🟢 P2 —— 打磨项（MVP 可暂缓）

| 缺口 | 说明 |
|------|------|
| 犯规规则不完整 | 中式八球的「开球未过中线」「连续三次犯规判负」「贴球」等 |
| 无局数/胜场统计 | 没有跨局数据持久化 |
| 断线重连无房间恢复 | `reconnecting` 只改 UI 状态点颜色 |
| BOSS 挑战占位 | `comingSoon`，未开发 |
| 无移动端安全区适配 | 刘海屏底部面板可能被遮 |
| 观战/房间列表 | 不在 MVP 范围内 |

---

## 3. 关键代码定位

以下标注了每个缺口对应的源码位置，方便后续修改：

### P0-1: Spin 无物理效果
```
UI 选择器: src/pages/pvp/index.vue:68-80   (9宫格)
渲染持有值: src/engine/renderer/CanvasRenderer.ts:48  (spinType)
击球入口:   src/stores/gameStore.ts:250-258  (localShoot 只传 power/angle)
球物理:     src/engine/physics/Ball.ts  (shoot 只设线速度，无角速度)
```

### P0-2: 在线无 Rematch
```
TODO:  src/stores/gameStore.ts:222-227  (rematch_request / rematch_accept)
按钮隐藏: src/pages/pvp/index.vue:113  (v-if="!isOnlineMode")
```

### P0-3: 对手断线无处理
```
事件源: src/network/NetworkManager.ts:222-225  (emit opponent_left)
未监听: src/stores/gameStore.ts:177-193  (handleNetworkEvent 只处理 connection_state + game_message)
```

### P0-4: 状态漂移
```
检测: src/stores/gameStore.ts:210-214  (checksum 不匹配时 console.warn)
TODO: src/stores/gameStore.ts:213  (// TODO: 状态纠正逻辑)
```

### P1-5: 瞄准辅助
```
瞄准线: src/engine/renderer/CanvasRenderer.ts  (aimLine: {start, end})
控制器: src/engine/input/CueController.ts  (getAimLineEnd)
```

### P1-6: 无音效
```
全局: 无任何音频文件或 AudioContext 使用
碰撞钩子: src/engine/physics/PhysicsWorld.ts  (有 postCollisionHooks / pocketHooks 雏形)
```

### P1-7: 换手无提示
```
换手: src/stores/gameStore.ts:354-356  (currentPlayerIndex 切换)
UI:  src/pages/pvp/index.vue:26  (只显示文字 "XX的回合")
```

### P1-8: WS 回退不可用
```
回退: src/network/NetworkManager.ts:172-177  (sendGameMessage WS 分支)
服务器: server/src/index.ts  (只 relay webrtc_offer/answer/ice_candidate)
```

---

## 4. 推荐收敛路线

### 路线 A：先做「本地对战 MVP」（推荐，3-5 天）

投入小、验证快，绕开所有在线同步难题。完成以下后即可让两个真人面对面玩一局完整的台球：

```
Step 1  隐藏 spin 控件（或实现最小旋转物理）     0.5-1 天
Step 2  本地换手「传手机」遮罩提示               0.5 天
Step 3  基础音效（击球/碰撞/进袋）              1-2 天
Step 4  瞄准辅助（库边一次反弹预测线）           0.5-1 天
Step 5  整体手感调优（物理参数微调 + UI 边界情况） 0.5 天
```

### 路线 B：再做「在线对战 MVP」（路线 A 之后，5-7 天）

```
Step 6  opponent_left / 断线检测 + 退出提示       0.5 天
Step 7  rematch 再来一局闭环                      1 天
Step 8  WS 回退修复（服务器支持游戏消息 relay）    1 天
Step 9  状态纠正（checksum 漂移时以房主为准覆盖）  2-3 天  ← 技术难点
Step 10 在线端到端联调 + 异常场景测试              1 天
```

### 路线 C：体验打磨（MVP 之后）

- 旋转物理（真正的角速度 + 走位效果）
- 进阶犯规规则
- 力场系统接入
- 局数/胜场统计
- 移动端安全区

---

## 5. 技术难点备注

### 状态同步（P0-4）的两种解法

**方案 A：权威主机模式（推荐）**
- 房主运行物理，每帧把所有球位置/速度同步给客机
- 客机只做渲染，不跑物理
- 优点：零漂移；缺点：带宽大（16球 × 2分量 ≈ 200 字节/帧，60fps ≈ 12KB/s），延迟体感明显

**方案 B：输入同步 + 漂移纠正（当前方案的补全）**
- 双方各自跑物理，只同步输入（power + angle）
- checksum 不匹配时，房主把完整状态快照发给客机，客机覆盖本地
- 优点：延迟低、带宽小；缺点：纠正瞬间有画面跳变

**MVP 建议**：先用方案 B，漂移纠正用「房主快照覆盖」即可。测试发现漂移频率如果高于每 5 局 1 次，再降级到方案 A。

### 旋转物理（P0-1 补充）

真正的旋转需要：
- `Ball` 新增 `angularVelocity`（角速度）和 `spinDirection`（旋转轴）
- `shoot()` 根据 spinType 对初始速度加偏移分量（顶部旋转 → 球前进后加速/减速；侧旋 → 碰库边偏转）
- 物理步进中处理旋转对速度的持续影响
- MVP 可简化为：只影响 **碰库边后的反射角偏移**，忽略滚动摩擦

---

## 6. 总结

| 维度 | 评估 |
|------|------|
| **本地对战离 MVP** | **很近** — 只差 spin 处理 + 换手提示 + 音效 + 瞄准辅助，约 3-5 天 |
| **在线对战离 MVP** | **较远** — 还差 rematch + 断线处理 + 状态纠正，约 5-7 天 |
| **最大技术风险** | 在线状态同步漂移纠正（P0-4），这是 P2P 架构的固有问题 |
| **最快出成果的路径** | 先做本地对战 MVP，让身边两人能玩起来，再逐步补在线能力 |

---

## 7. 执行顺序

> 原则：**先打通本地，再补全在线，最后打磨体验**。每个任务独立可验证，完成一项即可提交，出问题也方便回滚。

---

### 阶段一：本地对战 MVP（目标：两人面对面能玩完整一局）

#### Task 1 · 清理假 spin 控件
**目标**：消除「操作了但没用」的体验欺骗。  
**做法**：将底部 9 宫格击球部位选择器替换为一个说明文字（「旋转功能开发中」），或直接隐藏整行。不做物理接入，留给阶段三打磨。  
**验收**：UI 上不再出现无效控件，底部面板空间可分配给其他信息。  
**涉及文件**：`src/pages/pvp/index.vue`（删除 spin-selector 区块）

---

#### Task 2 · 换手遮罩提示
**目标**：本地双人同屏，换手时明确告知「现在该谁操作」，防止误击。  
**做法**：在 `pvp/index.vue` 添加全屏半透明遮罩，当 `currentPlayerIndex` 切换后显示「现在是 [玩家名] 的回合，请将手机递给 TA」+ 「我已准备好」按钮（点击后消失）。遮罩需覆盖 Canvas，阻断所有触摸事件。  
**验收**：每次换手出现遮罩，点击确认后消失，新玩家才能开始瞄准。在线模式下不显示此遮罩（各用各设备）。  
**涉及文件**：`src/pages/pvp/index.vue`

---

#### Task 3 · 瞄准辅助增强
**目标**：在手机触屏下也能相对准确地瞄准。  
**做法（由简到难，可分步实现）**：
1. **第一步（必做）**：瞄准线延伸到库边，并画出**库边一次反弹**后的折射线（用反射向量计算）。
2. **第二步（可选）**：在白球击打方向上，若碰到第一颗目标球，画出目标球的**预计运动方向线**（利用弹性碰撞公式预测法向分量方向）。  

**验收**：瞄准时能看到至少一次库边反弹预测线；瞄到目标球时能看到目标球的预测方向。  
**涉及文件**：`src/engine/renderer/CanvasRenderer.ts`（新增预测线绘制）、`src/engine/input/CueController.ts`（传递额外预测数据）

---

#### Task 4 · 基础音效
**目标**：加上击球声、球球碰撞声、进袋声，建立台球手感。  
**做法**：
- 用 `AudioContext` + `fetch` 加载本地音频文件（`.ogg` / `.mp3`），存入 `src/static/sounds/`。
- 或用 Web Audio API 程序化合成简单音效（击球 → 短促冲击波；碰撞 → 频率随速度变化的碰撞音；进袋 → 低沉落袋音）。程序化合成无需音频资产，更易维护。
- 在 `PhysicsWorld` 的 `postCollisionHooks` / `pocketHooks` 里触发音效。  

**验收**：击球、碰撞（力度越大声音越响）、进袋三种声音均能在浏览器中播放。  
**涉及文件**：新建 `src/engine/audio/AudioEngine.ts`，挂载到 `src/pages/pvp/index.vue`

---

#### Task 5 · 本地对战整体手感调优
**目标**：跑一局完整游戏，把体验不顺的地方调整到位。  
**重点检查**：
- 球桌缩放/居中是否在不同屏幕尺寸（横屏/竖屏、宽屏/窄屏）下都正确
- 移动端安全区（刘海屏底部）是否遮挡底部面板 → 加 `padding-bottom: env(safe-area-inset-bottom)`
- 蓄力/击球手感（power 范围、击球动画速度）是否合理
- 犯规提示（foulMessage）是否足够明显，是否会和其他 UI 重叠  

**验收**：在手机浏览器（或模拟器）上完整打完一局，无布局崩溃、无死锁。

---

**阶段一里程碑**：完成 Task 1-5 后，两个真人可以面对面传手机玩完整局，所有 UI 反馈清晰，无明显 bug。

---

### 阶段二：在线对战 MVP（目标：两人通过网络能玩完整一局）

#### Task 6 · 对手断线/退出处理
**目标**：对手中途退出时，己方不死锁。  
**做法**：在 `gameStore.handleNetworkEvent` 里监听 `opponent_left` 事件，触发后：
1. 暂停游戏循环
2. 显示全屏提示「对手已离开游戏」+ 「返回首页」按钮
3. 调用 `leaveOnlineMode()` 清理网络状态  

**验收**：对手关闭标签页后，己方 5 秒内看到提示并能正常返回首页。  
**涉及文件**：`src/stores/gameStore.ts`（handleNetworkEvent）、`src/pages/pvp/index.vue`（新增断线弹窗）

---

#### Task 7 · 在线「再来一局」闭环
**目标**：在线局结束后，双方可以协商重开，而非只能回首页。  
**做法**：
1. game over 弹窗在线模式下显示「请求再来一局」按钮（原来是 `v-if="!isOnlineMode"` 隐藏）
2. 点击后发送 `rematch_request` 消息给对手
3. 对手收到后弹出「对手请求再来一局」确认框
4. 双方均确认后，发送 `rematch_accept`，双方各自调用 `initGame()` 重新开始
5. 若对手拒绝/超时未响应，发起方收到提示  

**验收**：在线一局结束后可以在不断开 P2P 连接的情况下重开新局。  
**涉及文件**：`src/stores/gameStore.ts`（实现 rematch TODO）、`src/pages/pvp/index.vue`（rematch UI）

---

#### Task 8 · WebSocket 消息中转修复
**目标**：在 WebRTC 不可用时（移动端 NAT 穿透失败），游戏消息也能通过信令服务器中转。  
**做法**：
1. 服务器 `server/src/index.ts` 新增处理 `relay_game_message` 消息类型：收到后原样转发给对手
2. 客户端 `NetworkManager.sendGameMessage` 的 WS 回退分支改为发送 `{ type: 'relay_game_message', payload: msg }`
3. 服务器收到后发给对手的 `game_message` 事件  

**验收**：在禁用 WebRTC 的环境下（可用 Chrome flag 模拟），在线游戏仍可正常进行。  
**涉及文件**：`server/src/index.ts`、`server/src/types.ts`、`src/network/NetworkManager.ts`

---

#### Task 9 · 物理状态同步纠正（方案 B：房主快照覆盖）
**目标**：消除多回合后的状态漂移，确保双方看到同一个球局。  
**做法**：
1. 每回合结束时，房主（`isHost`）序列化所有球的状态（位置、速度、`active`）为 JSON 快照
2. 通过 `sendGameMessage` 发送 `{ type: 'state_snapshot', balls: [...] }` 给客机
3. 客机收到快照后，覆盖本地 `physicsWorld.balls` 对应的位置/速度
4. 若 checksum 不匹配（之前的检测逻辑），立即向房主请求快照  

**注意**：快照覆盖可能导致瞬间跳变 → 可加 0.2s 的平滑插值过渡来减弱视觉突兀感。  
**验收**：连续打 10 回合，两端球局始终一致（肉眼无差异）。  
**涉及文件**：`src/stores/gameStore.ts`（新增 state_snapshot 消息处理）、`src/network/types.ts`（新增消息类型）

---

#### Task 10 · 在线端到端联调与异常场景测试
**目标**：在真实网络环境下把上面 5 个任务跑通，修掉联调中暴露的问题。  
**测试矩阵**：
- 正常流程：创建房间 → 加入 → 开始 → 打完一局 → 再来一局
- 异常流程：对手中途退出、网络抖动、连续犯规、8号球提前进袋
- 设备组合：Chrome PC + Chrome PC、Chrome PC + 手机浏览器、两台手机  

**验收**：上述所有场景无死锁、无未处理异常，体验流畅。

---

**阶段二里程碑**：完成 Task 6-10 后，两个人可以通过链接联机，玩完整局（含断线处理、rematch），球局状态基本一致。

---

### 阶段三：体验打磨（MVP 之后，按需推进）

> 以下任务不阻塞 MVP 发布，可在 MVP 稳定后按兴趣或玩家反馈决定顺序。

| 任务 | 说明 | 预计工作量 |
|------|------|----------|
| **T11 旋转物理** | Ball 新增角速度，shoot() 接入 spinType，实现顶旋/底旋/侧旋对运动轨迹的影响 | 2-3 天 |
| **T12 力场系统接入** | 把 `ForceField.ts` 的雏形接入游戏流程，设计触发时机（例如每隔几回合随机激活一个力场格） | 3-5 天 |
| **T13 犯规规则完善** | 补充「开球未过中线」「连续犯规」「贴球」等中式八球规则 | 1-2 天 |
| **T14 局数/胜场统计** | 跨局数据持久化（localStorage），首页显示历史战绩 | 1 天 |
| **T15 移动端安全区** | 全局加 `safe-area-inset` 适配，测试 iPhone/Android 刘海屏 | 0.5 天 |
| **T16 断线重连恢复** | 重连后房主发送完整游戏状态，客机恢复局面继续游戏 | 2-3 天 |

---

### 总览时间线

```
阶段一（本地对战 MVP）        约 3-5 天
  Task 1  清理假 spin 控件             0.5 天
  Task 2  换手遮罩提示                 0.5 天
  Task 3  瞄准辅助增强                 1 天
  Task 4  基础音效                     1-2 天
  Task 5  整体手感调优                 0.5 天

阶段二（在线对战 MVP）        约 5-7 天
  Task 6  断线/退出处理                0.5 天
  Task 7  在线再来一局                 1 天
  Task 8  WS 中转修复                  1 天
  Task 9  状态同步纠正                 2-3 天
  Task 10 端到端联调                   1 天

阶段三（体验打磨）            按需推进
  T11-T16 旋转物理、力场、规则完善等
```

> **建议每完成一个 Task 就 `git commit` 一次，部署到 Vercel 验证**，这样每个节点都有一个可展示的可玩版本。
